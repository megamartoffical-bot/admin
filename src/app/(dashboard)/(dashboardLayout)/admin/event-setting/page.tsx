import EventsSettings from '@/components/pages/admin/EventsSettings'

export const metadata = {
  title: 'Event Settings | Meta Mart',
  description: 'Manage and configure event settings efficiently.',
}

const page = () => {
  return <EventsSettings />
}

export default page
