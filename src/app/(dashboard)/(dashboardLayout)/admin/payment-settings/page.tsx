import PaymentSettings from '@/components/pages/admin/PaymentSettings'

export const metadata = {
  title: 'Payment Settings | Meta Mart',
  description: 'Manage and configure payment settings for your Meta Mart platform.',
}

const page = () => {
  return <PaymentSettings />
}

export default page
