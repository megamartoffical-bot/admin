import SettingsPage from '@/components/pages/admin/SettingsPage'

export const metadata = {
  title: 'General Settings | Meta Mart',
  description: 'Manage and configure general settings for your Meta Mart admin panel.',
}

const page = () => {
  return <SettingsPage />
}

export default page
