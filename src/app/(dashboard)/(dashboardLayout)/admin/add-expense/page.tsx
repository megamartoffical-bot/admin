import AddExpensePage from '@/components/pages/admin/AddExpensePage';
export const metadata = {
  title: "Add Expense | CartX",
  description: "Add a new expense in the admin dashboard.",
};

const page = () => {
  return <AddExpensePage />;
};

export default page;
