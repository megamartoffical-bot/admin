import AddProductPage from '@/components/pages/admin/AddNewProductPage';

export const metadata = {
  title: "Add Product | CartX",
  description: "Add a new product in the admin dashboard.",
};

const page = () => {
  return (
    <AddProductPage />
  )
}

export default page
