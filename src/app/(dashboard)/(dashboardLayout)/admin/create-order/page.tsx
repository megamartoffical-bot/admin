import CreateOrder from '@/components/pages/admin/CreateOrder'

export const metadata = {
  title: 'Create Order | Meta Mart',
  description: 'Create and manage orders efficiently with our user-friendly order creation interface.',
}

const page = () => {
  return (
    <CreateOrder />
  )
}

export default page
